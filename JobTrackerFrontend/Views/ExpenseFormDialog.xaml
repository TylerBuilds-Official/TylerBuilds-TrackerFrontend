<Window x:Class="JobTrackerFrontend.Views.ExpenseFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:JobTrackerFrontend.ViewModels"
        d:DataContext="{d:DesignInstance vm:ExpenseFormViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{md:MaterialDesignFont}"
        Loaded="OnLoaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="700">
        <StackPanel Margin="24">

            <!-- Category -->
            <ComboBox ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      DisplayMemberPath="Name"
                      md:HintAssist.Hint="Category *"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      Margin="0,0,0,16" />

            <!-- Job (optional) -->
            <ComboBox ItemsSource="{Binding Jobs}"
                      SelectedItem="{Binding SelectedJob}"
                      md:HintAssist.Hint="Job (optional — leave blank for general expense)"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      Margin="0,0,0,16">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding Title}" />
                            <Run Text=" — " />
                            <Run Text="{Binding ClientName}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <!-- Vendor -->
            <TextBox Text="{Binding Vendor, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Vendor *"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,16" />

            <!-- Description -->
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Description"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,16" />

            <!-- Amount + Date -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged}"
                         md:HintAssist.Hint="Amount *"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,8,0" />

                <DatePicker Grid.Column="1"
                            SelectedDate="{Binding ExpenseDate}"
                            md:HintAssist.Hint="Date *"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                            Margin="8,0,0,0" />
            </Grid>

            <!-- Reimbursable -->
            <CheckBox Content="Reimbursable (will be billed back to client)"
                      IsChecked="{Binding IsReimbursable}"
                      Style="{StaticResource MaterialDesignCheckBox}"
                      Margin="0,0,0,16" />

            <!-- Receipt File -->
            <DockPanel Margin="0,0,0,16">
                <Button DockPanel.Dock="Right"
                        Content="Browse"
                        Command="{Binding BrowseReceiptCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="8,0,0,0"
                        Padding="12,4"
                        VerticalAlignment="Center" />
                <TextBox Text="{Binding ReceiptFilePath, UpdateSourceTrigger=PropertyChanged}"
                         md:HintAssist.Hint="Receipt File Path (optional)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         IsReadOnly="True" />
            </DockPanel>

            <!-- Notes -->
            <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Notes"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MinHeight="60"
                     MaxHeight="120"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,16" />

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}"
                       Margin="0,0,0,12"
                       TextWrapping="Wrap" />

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Command="{Binding CancelCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,12,0" />
                <Button Content="Save"
                        Command="{Binding SaveCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBooleanConverter}}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
