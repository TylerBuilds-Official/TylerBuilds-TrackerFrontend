<Window x:Class="JobTrackerFrontend.Views.ResearchThemeManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:JobTrackerFrontend.ViewModels"
        d:DataContext="{d:DesignInstance vm:ResearchThemeManagerViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Manage Research Themes"
        Width="560" Height="480"
        MinWidth="460" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{md:MaterialDesignFont}"
        Loaded="OnLoaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Loading -->
        <ProgressBar Grid.Row="0"
                     Style="{StaticResource MaterialDesignLinearProgressBar}"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Height="2" Margin="0,0,0,4"
                     Background="Transparent" BorderThickness="0" />

        <!-- Themes Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Themes}"
                  SelectedItem="{Binding SelectedTheme}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  MinHeight="100"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Grid Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,12">
            <Button Content="Edit"
                    Command="{Binding EditThemeCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsEnabled="{Binding SelectedTheme, Converter={StaticResource NotNullToBoolConverter}}"
                    Margin="0,0,8,0" Padding="16,4" />
            <Button Content="Delete"
                    Command="{Binding DeleteThemeCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsEnabled="{Binding SelectedTheme, Converter={StaticResource NotNullToBoolConverter}}"
                    Padding="16,4" />
        </StackPanel>

        <!-- Separator -->
        <Separator Grid.Row="3" Margin="0,0,0,12" />

        <!-- Add / Edit Form -->
        <StackPanel Grid.Row="4" Margin="0,0,0,16">

            <!-- Editing indicator -->
            <TextBlock Margin="0,0,0,8"
                       Opacity="0.7" FontSize="12"
                       Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Run Text="Editing theme — " />
                <Hyperlink Command="{Binding CancelEditCommand}">
                    <Run Text="Cancel" />
                </Hyperlink>
            </TextBlock>

            <!-- Name -->
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Theme Name *"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12" />

            <!-- Description + Save -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         md:HintAssist.Hint="Description (optional)"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,12,0" />

                <Button Grid.Column="1"
                        Content="{Binding SaveButtonText}"
                        Command="{Binding SaveThemeCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        VerticalAlignment="Center"
                        Padding="20,6" />
            </Grid>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}"
                       Margin="0,8,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Footer -->
        <DockPanel Grid.Row="5">
            <Button Content="Close"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right"
                    Padding="20,6" />
        </DockPanel>
    </Grid>
</Window>
