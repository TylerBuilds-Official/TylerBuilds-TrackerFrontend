<Window x:Class="JobTrackerFrontend.Views.LineItemsManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:JobTrackerFrontend.ViewModels"
        d:DataContext="{d:DesignInstance vm:LineItemsManagerViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Width="640" Height="520"
        MinWidth="540" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{md:MaterialDesignFont}"
        Loaded="OnLoaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Loading -->
        <ProgressBar Grid.Row="0"
                     Style="{StaticResource MaterialDesignLinearProgressBar}"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Height="2" Margin="0,0,0,4"
                     Background="Transparent" BorderThickness="0" />

        <!-- Line Items Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding LineItems}"
                  SelectedItem="{Binding SelectedLineItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  MinHeight="120"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*" />
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat=N2}" Width="*" />
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="*" />
                <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat=C}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Grid Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,12">
            <Button Content="Edit"
                    Command="{Binding EditItemCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsEnabled="{Binding SelectedLineItem, Converter={StaticResource NotNullToBoolConverter}}"
                    Margin="0,0,8,0" Padding="16,4" />
            <Button Content="Delete"
                    Command="{Binding DeleteItemCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsEnabled="{Binding SelectedLineItem, Converter={StaticResource NotNullToBoolConverter}}"
                    Padding="16,4" />
        </StackPanel>

        <!-- Separator -->
        <Separator Grid.Row="3" Margin="0,0,0,12" />

        <!-- Add / Edit Form -->
        <StackPanel Grid.Row="4" Margin="0,0,0,16">

            <!-- Editing indicator -->
            <TextBlock Margin="0,0,0,8"
                       Opacity="0.7" FontSize="12"
                       Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Run Text="Editing line item — " />
                <Hyperlink Command="{Binding CancelEditCommand}">
                    <Run Text="Cancel" />
                </Hyperlink>
            </TextBlock>

            <!-- Description -->
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     md:HintAssist.Hint="Description *"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12" />

            <!-- Qty + Unit Price + Save -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                         md:HintAssist.Hint="Quantity"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,8,0" />

                <TextBox Grid.Column="1"
                         Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                         md:HintAssist.Hint="Unit Price *"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="8,0,12,0" />

                <Button Grid.Column="2"
                        Content="{Binding SaveButtonText}"
                        Command="{Binding SaveItemCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        VerticalAlignment="Center"
                        Padding="20,6" />
            </Grid>

            <!-- Error Message -->
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}"
                       Margin="0,8,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Footer: Total + Close -->
        <DockPanel Grid.Row="5">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Total: " Opacity="0.6" FontSize="14" VerticalAlignment="Center" />
                <TextBlock Text="{Binding GrandTotal, StringFormat=C}"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>

            <Button Content="Close"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right"
                    Padding="20,6" />
        </DockPanel>
    </Grid>
</Window>
