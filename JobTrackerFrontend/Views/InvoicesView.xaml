<UserControl x:Class="JobTrackerFrontend.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:JobTrackerFrontend.ViewModels"
             d:DataContext="{d:DesignInstance vm:InvoicesViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Loaded="OnLoaded">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Loading -->
        <ProgressBar Grid.Row="0"
                     Style="{StaticResource MaterialDesignLinearProgressBar}"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Height="2"
                     Margin="0,0,0,4"
                     Background="Transparent"
                     BorderThickness="0" />

        <!-- Header -->
        <DockPanel Grid.Row="1" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Open Invoice"
                        Command="{Binding OpenInvoiceFileCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,12,0"
                        IsEnabled="{Binding SelectedInvoice.NetworkFilePath, Converter={StaticResource NotNullToBoolConverter}}" />
                <Button Content="Delete"
                        Command="{Binding DeleteInvoiceCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,12,0"
                        IsEnabled="{Binding SelectedInvoice, Converter={StaticResource NotNullToBoolConverter}}" />
                <Button Content="Edit"
                        Command="{Binding EditInvoiceCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,12,0"
                        IsEnabled="{Binding SelectedInvoice, Converter={StaticResource NotNullToBoolConverter}}" />
                <Button Content="New Invoice"
                        Command="{Binding NewInvoiceCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>
        </DockPanel>

        <!-- Status Filter -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <ComboBox Width="160"
                      md:HintAssist.Hint="Filter by status"
                      SelectedItem="{Binding StatusFilter}"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      Padding="8,4" FontSize="13">
                <ComboBoxItem Content="" />
                <ComboBoxItem Content="Draft" />
                <ComboBoxItem Content="Sent" />
                <ComboBoxItem Content="Partially Paid" />
                <ComboBoxItem Content="Paid" />
                <ComboBoxItem Content="Overdue" />
                <ComboBoxItem Content="Cancelled" />
            </ComboBox>
        </StackPanel>

        <!-- Invoice List -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Invoices}"
                  SelectedItem="{Binding SelectedInvoice}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  AlternationCount="2"
                  AlternatingRowBackground="#0A455A64"
                  MouseDoubleClick="OnRowDoubleClick">
            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Set Status">
                        <MenuItem Header="Draft" Command="{Binding SetStatusCommand}" CommandParameter="Draft" />
                        <MenuItem Header="Sent" Command="{Binding SetStatusCommand}" CommandParameter="Sent" />
                        <MenuItem Header="Partially Paid" Command="{Binding SetStatusCommand}" CommandParameter="Partially Paid" />
                        <MenuItem Header="Paid" Command="{Binding SetStatusCommand}" CommandParameter="Paid" />
                        <MenuItem Header="Overdue" Command="{Binding SetStatusCommand}" CommandParameter="Overdue" />
                        <MenuItem Header="Cancelled" Command="{Binding SetStatusCommand}" CommandParameter="Cancelled" />
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Edit" Command="{Binding EditInvoiceCommand}" />
                    <MenuItem Header="Record Payment" Command="{Binding RecordPaymentCommand}"
                              IsEnabled="{Binding CanRecordPayment}" />
                    <MenuItem Header="Open Invoice File" Command="{Binding OpenInvoiceFileCommand}" />
                    <Separator />
                    <MenuItem Header="Delete" Command="{Binding DeleteInvoiceCommand}" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice #" Binding="{Binding DisplayNumber}" Width="1.5*" />
                <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="2*" />
                <DataGridTextColumn Header="Job" Binding="{Binding JobTitle}" Width="2*" />
                <DataGridTemplateColumn Header="Status" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8"
                                    Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"
                                    Padding="8,2" Margin="0,2"
                                    HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Status}"
                                           Foreground="{Binding Status, Converter={StaticResource StatusToForegroundConverter}}"
                                           FontSize="11" FontWeight="Medium" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="*" />
                <DataGridTextColumn Header="Paid" Binding="{Binding TotalPaid, StringFormat=C}" Width="*" />
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=d}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
